/**
 * Fenêtre unique : rétention merge RSS + filtre accueil « récents ».
 * Une seule source pour front + serveur.
 */
export const RSS_HOME_HOURS = 24;

/** Alias métier (merge-retention) — même valeur. */
export const RSS_ITEM_RETENTION_HOURS = RSS_HOME_HOURS;
