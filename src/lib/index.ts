/**
 * Point d'entrée principal pour tous les services de l'application
 * Réexporte les services depuis le répertoire services pour maintenir
 * la compatibilité avec la nouvelle structure
 */

// Réexporter tous les services depuis le répertoire services
export * from '../services';
