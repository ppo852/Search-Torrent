/**
 * Noms canoniques qBittorrent — réexport shared (compat imports serveur).
 */
export {
  QBIT_CATEGORIES,
  normalizeQbitCategory,
  inferQbitCategoryFromMediaType,
  resolveQbitCategory,
} from '../../../shared/qbit-categories.js';
